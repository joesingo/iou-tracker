{
   "_id": "_design/iou",
   "views": {
       "getious": {
           "map": "function(doc){if (doc.people && doc.people.length == 2){emit(doc.people[0], doc);emit(doc.people[1], doc);}}"
       },
       "getiou": {
           "map": "function(doc){if (doc.people && doc.people.length == 2){emit([doc.people[0], doc.people[1]], doc);emit([doc.people[1], doc.people[0]], doc);}}"
       }
   }
}